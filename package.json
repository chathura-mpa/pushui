{
    "name": "mpk-ui-blueprint",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "dev": "npm run dev --workspace=packages/cli"
    },
    "devDependencies": {
        "typescript": "^5.3.3"
    }
}